interface:
  display_name: "钉钉 OA 历史审批归档"
  short_description: "从 CLI 安装登录到审批分类导出，支持 macOS、Windows 和中断续传"
  default_prompt: "使用 $dingtalk-oa-history-analysis 检查钉钉环境，将我的历史审批按类型整理为 XLSX。"
policy:
  allow_implicit_invocation: true
